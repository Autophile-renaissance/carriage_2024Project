class Address{
  String placedFormattedAddress;
  String placeholder;
  String placeId;
  double latitude;
  double longitude;

  Address({ required this.placedFormattedAddress, required this.placeholder, required this.placeId, required this.latitude, required this.longitude});
}